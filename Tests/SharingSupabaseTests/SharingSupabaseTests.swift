import Sharing
import Testing

@testable import SharingSupabase

// struct Post: Decodable {}

@Test func example() async throws {
    // @SharedReader(
    //     .supabase(
    //         table: "posts",
    //         query: { @Sendable in $0.select() }
    //     )
    // ) var posts: [Post] = []
}
